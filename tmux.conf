# --- GENERAL SETTINGS ---
set -g default-terminal "screen-256color"
set -g history-limit 10000
set -g mouse on
set -s escape-time 0      # Faster key response
set -g base-index 1       # Start window numbering at 1
setw -g pane-base-index 1 # Start pane numbering at 1

# --- PREFIX ---
# Set prefix to Ctrl+Space
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# --- INTUITIVE BINDINGS ---

# Splitting: v for Vertical, h for Horizontal
unbind v
unbind h
bind v split-window -h -c "#{pane_current_path}"
bind h split-window -v -c "#{pane_current_path}"

# Windows: n for New, m for Next, p for Previous
unbind n
unbind p
bind n new-window -c "#{pane_current_path}"
bind m next-window
bind p previous-window

# Closing: x for Close (no confirmation)
unbind x
bind x kill-pane

# Reload config easily
bind r source-file ~/.config/tmux/tmux.conf \; display "Config Reloaded!"

# --- NAVIGATION (Vim-style) ---
# Note: Christoomey/vim-tmux-navigator handles Ctrl+h/j/k/l automatically

# --- NORMAL MODE (Copy Mode) ---
setw -g mode-keys vi

# Use Prefix + Escape to enter "Normal Mode"
bind Escape copy-mode

# Vim keys for selecting and copying
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# --- PLUGINS (TPM) ---
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'

# Theme
set -g @plugin 'catppuccin/tmux'
set -g @catppuccin_flavor 'mocha'

# Initialize TMUX plugin manager (KEEP THIS AT THE BOTTOM)
run '~/.tmux/plugins/tpm/tpm'
